#include "include"
/**
 * _isupper - checks if a character is capital or not
 * @c: This is the input to the code
 * Return: 1 if a character is uppercase and 0 if not
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0)
	_putchar('\n');
}
