#include "main.h"
#include <math.h>

/**
 * _sqrt_recursion - 
 * @n:
 * Return:
 */

int _sqrt_recursion(int n)
{
	int returns;

	if (n < 0)
		return (-1)
	else
	{
		returns = sqrt(n);
		return (returns);
	}
}
