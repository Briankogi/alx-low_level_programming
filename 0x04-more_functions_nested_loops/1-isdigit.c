#include "main.h"
/**
 * _isdigit - It checks for a digit in range of 0-9
 * @c: This is the variables name
 * Return: 1 if it fits in the range, o otherwise
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1)
	else
		return (0)
}
